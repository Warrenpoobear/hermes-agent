{
  "install": "workspace_dir=\"${workspaceFolder:-${WORKSPACE_FOLDER:-${WORKSPACE_PATH:-$PWD}}}\"; if [ -f \"${workspace_dir}/.venv/bin/activate\" ]; then source \"${workspace_dir}/.venv/bin/activate\"; elif [ -f \"${workspace_dir}/venv/bin/activate\" ]; then source \"${workspace_dir}/venv/bin/activate\"; else cd \"${workspace_dir}\" && if ! command -v hermes >/dev/null 2>&1; then printf 'n\\n' | ./setup-hermes.sh; fi && if [ -f \"${workspace_dir}/venv/bin/activate\" ]; then source \"${workspace_dir}/venv/bin/activate\"; fi; fi; hermes --version"
}
